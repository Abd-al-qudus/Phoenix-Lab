import 'package:flutter/material.dart';

class TestScreenBuilder extends StatelessWidget {
  const TestScreenBuilder({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Test Screen'),
    );
  }
}
